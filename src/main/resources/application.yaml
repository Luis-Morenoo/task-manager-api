spring:
  application:
    name: task-manager
  data:
    mongodb:
      uri: ${MONGODB_URI}

server:
  port: 8080

logging:
  level:
    root: INFO
    com.luis.taskmanager: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs